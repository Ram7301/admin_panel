// Re-export FONT_ARCHIVO from the central app config so the font loader runs
// only once. Kept here for backward compatibility with `@/themes/config` imports.
export { FONT_ARCHIVO } from '@/config';
